# Backend Server Configuration
PORT=5000
NODE_ENV=development

# SMTP Email Configuration
# Replace these with your actual SMTP credentials

# For Gmail SMTP (Recommended)
# Email transport configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_account@gmail.com
SMTP_PASS=your_app_password

# Notification recipient
NOTIFY_TO=recipient@example.com

# Public logo URL used in email headers (defaults to backend static asset if not provided)
# EMAIL_LOGO_URL is optional; if unset, emails will not include a logo

# For Outlook/Hotmail SMTP
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password

# For Yahoo SMTP
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@yahoo.com
# SMTP_PASS=your-app-password

# For custom SMTP (like cPanel, etc.)
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=noreply@yourdomain.com
# SMTP_PASS=your-password

# For SSL/TLS (Port 465)
# SMTP_PORT=465
# SMTP_SECURE=true

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace the values with your actual SMTP credentials
# 3. For Gmail: Enable 2FA and generate an App Password
#    - Go to Google Account Settings > Security > 2-Step Verification > App passwords
#    - Generate password for "Mail" and use it as SMTP_PASS
# 4. Never commit the .env file to version control
# 5. Test your configuration by running the backend server
